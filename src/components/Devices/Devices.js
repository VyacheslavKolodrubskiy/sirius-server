import Header from '../Header/Header';
import Sidebar from '../Sidebar';
import './Devices.scss';

const Devices = () => {
  return (
    <div class='devices'>
      <Header />
      <Sidebar />
    </div>
  );
};

export default Devices;
